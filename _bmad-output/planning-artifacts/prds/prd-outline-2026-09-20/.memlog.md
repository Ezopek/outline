---
topic: Deterministic Wiki Linting for Outline MCP
updated: 2026-09-21T00:55
---

- (decision) Create intent, Coaching path, and Vision + Features entry point selected explicitly by the user.
- (decision) The approved product brief is the primary product-decision source; its addendum supplies technical constraints for this PRD and later architecture, and neither source artifact may be modified.
- (event) PRD workspace initialized after source and repository-currentness review.
- (event) Brain dump completed: user has no additional context beyond the approved brief and addendum at this time.
- (decision) Stakes calibrated as an internal component intended for a real pilot and later production; PRD rigor should approach launch-grade for security, MCP contracts, and reliability.
- (decision) Vision positions the product as an agent-facing deterministic integrity contract for Outline MCP, not a general administrative Wiki linter.
- (change) Added Document Purpose and approved Vision sections to prd.md.
- (decision) Wiki operators are secondary decision users: they approve policy and delivery gates and receive escalations; agents remain the primary remediation actors.
- (change) Added Target Users, Jobs To Be Done, and initial non-user boundaries to prd.md.
- (decision) Read-only lint requests for unconfigured documents or collections return an explicit machine-readable policy-not-configured/not-evaluated outcome, never a clean ok result; unconfigured write behavior remains upstream-compatible.
- (decision) lint_wiki without collectionId evaluates all configured collections and documents readable by the current actor, reports the evaluated scope, and defines clean only within that authorized scope without revealing inaccessible resources.
- (decision) Read-only linting may preserve and return findings from successfully evaluated portions after a scoped runtime failure, continue independent safe checks, and return separate systemFailures with complete=false; such a result is never clean.
- (decision) User delegated result-ordering judgment. Findings use separate errors, warnings, and systemFailures arrays and deterministic document-centric ordering by stable scope identifiers, then precise location, then issue code; architecture may choose the mechanism but not alter observable order.
- (decision) Internal-link diagnostics use access-aware collapsing: missing, deleted, and inaccessible targets share a non-disclosing outcome whenever a distinction would reveal protected information; specificity is allowed only for already-authorized state.
- (change) Added user journeys, glossary, FR-1 through FR-10 for deterministic results and read-only linting, plus measurable capability acceptance criteria.
- (change) Created English PRD addendum with fixed architecture inputs and explicitly deferred implementation decisions; approved brief artifacts remain unchanged.
- (decision) Runtime-failure fail-open is an explicit per-collection server-side policy, defaults off, cannot be requested or overridden by MCP clients, never applies to invalid configuration, and always emits degradedValidation when exercised.
- (decision) Every configured create/update response carries one explicit validation status: validated, validated_with_warnings, degraded_validation, or rejected; unconfigured write responses preserve upstream behavior.
- (change) Added FR-11 through FR-18 covering per-collection policy, configuration and runtime failure behavior, complete projected-document enforcement, explicit write outcomes, atomic rejection, and the MCP-only enforcement boundary.
- (decision) Performance scale input: current pilot collection has 46 documents; typical collections are expected to contain about 50–150 documents and may continue growing without a fixed final size.
- (assumption) Use 150 documents as the normal acceptance profile and 1,000 documents as a growth guardrail profile unless the user revises the proposed budgets.
- (override) Performance-budget overruns are implementation/NFR failures, not integrity Errors and do not invalidate a successfully completed Clean Result; only an actual timeout, interruption, or incomplete evaluation produces a System Failure and complete=false.
- (decision) Deterministic collection-size or reference-density warnings are not added to the initial lint contract because the approved brief excludes Wiki quality statistics; duration and processed-scope telemetry belong to observability, with such warnings parked for a later product decision.
- (decision) Performance budgets are implementation-readiness NFRs only: completed slow lint may still be clean; only timeout, interruption, or incomplete evaluation yields a System Failure and complete=false. Size/density warnings remain deferred per the brief.
- (change) Added NFR-1 through NFR-5 for document latency, write overhead, normal and growth Wiki profiles, proportional scaling, and reproducible benchmark evidence.
- (decision) A Clean Result must describe one coherent point-in-time state; if concurrent changes make coherence unverifiable, lint_wiki returns complete=false with a System Failure, while architecture chooses the consistency mechanism.
- (change) Added NFR-6 through NFR-26 for consistency, reliability, security, observability, compatibility, maintainability, and environment independence; deferred consistency mechanism to the addendum.
- (event) Measured-pilot audit review: baseline persists MCP document mutations in Event records with authType=mcp and traces every MCP tool call, but does not persist read-only lint outcomes, validation status/warnings, logical batch boundaries, or reliable client identity; logs/database alone cannot prove all pilot metrics.
- (decision) Use Outline existing authorized Event/audit-log infrastructure as the durable evidence source for lint_document, lint_wiki, and configured write-validation outcomes; external telemetry is currently disabled and is not required for initial acceptance.
- (decision) Persist audit-safe summaries only: operation/outcome, completion, duration, authorized scope, policy fingerprint, correlation, evaluated-object counts, and Finding counts by severity/code; never full findings, content, messages, locations, protected targets, raw configuration, or request bodies.
- (override) Rejected validation attempts may and must leave a privacy-preserving audit record; no-side-effect requirements prohibit document mutation and successful-mutation signals, not rejection audit evidence.
- (change) Added FR-19 through FR-21 for durable audit evidence, updated observability NFRs, and added audit constraints and deferred schema decisions to the PRD addendum.
- (override) Audit persistence failures are operator concerns only: they do not change lint outcomes, block or roll back writes, or appear in standard MCP responses; pilot diagnostics capture evidence gaps out of band.
- (change) Added FR-22 and NFR-27 for audit failure isolation and constrained architecture to keep audit persistence outside the functional transaction failure domain.
- (decision) Pilot evidence threshold accepted in principle: at least seven days, ten declared logical batches, and two supported MCP clients, with measurement split across authorized audit summaries, restricted pilot capture, automated security tests, and operator adjudication.
- (change) Added separate implementation-readiness and pilot success metrics SM-I1–SM-I7 and SM-P1–SM-P7, plus counter-metrics SM-C1–SM-C7 with explicit evidence sources.
- (change) Added initial implementation sequencing, separately approved stages, explicit non-goals, risk guardrails, and dependencies to prd.md.
- (decision) Deployment runs the approved image with collections unconfigured or lint policy inactive; production rollout separately enables named collections, first read-only remediation and pilot evidence, then hard MCP enforcement.
- (change) Added four explicit approval gates for implementation readiness, image publication, deployment, and production rollout with bounded entry, pass, and exit authority.
- (decision) Issue severity is invariant per stable issue code; collection policy may select rules and deterministic parameters but cannot override severity, and advisory/blocking variants require separate codes.
- (change) Added FR-23 for stable severity semantics and an explicit downstream decision register OD-1–OD-7 with owners and gate deadlines; no unconfirmed product assumptions remain.
- (decision) The 150-document normal profile and 1,000-document growth guardrail were confirmed as implementation-readiness performance budgets, subject to reproducible baseline benchmark definition.
- (event) Memlog audit completed: all product decisions and overrides are represented in prd.md or addendum.md; activation/workspace events remain audit-only, and no unresolved assumptions or PM notes remain.
- (decision) Write-policy applicability follows the Projected Document's post-operation collection: entering configured scope is validated before persistence; leaving configured scope is not blocked by the former collection's content policy; other mutation tools remain outside initial enforcement and are detected by lint_wiki.
- (change) Reviewer gate repairs: audit is attempted for every operation and durable under normal audit operation while persistence failures remain isolated; NFR-5 requires normalized growth evidence rather than an undefined proportionality pass/fail claim; stable IDs explicitly do not encode presentation order.
- (event) Open-item triage completed: no assumptions, PM notes, or unresolved product-policy decisions remain; OD-1 through OD-7 are bounded downstream decisions with owners and gate deadlines.
- (change) Applied final structure and prose polish to prd.md and addendum.md; preserved the launch-grade capability shape and architecture handoff while clarifying Projected Document scope, upstream checks, and audit attribution.
- (event) PRD finalized
